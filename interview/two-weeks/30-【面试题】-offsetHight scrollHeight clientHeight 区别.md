> 盒子模型

```
width height padding magin boder box-sizing
```

> 计算规则

```
offsetHeight offsetWidth: border + padding + content
clientHeight clientWidth：padding + content
scrollHeight scrollWidth: padding + 实际内容尺寸
```